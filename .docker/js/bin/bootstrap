#!/usr/bin/env bash

source $ROOT_DIR/.docker/.shared/bin/bootstrap

VERSION=$GRAPL_NODE_VERSION
APP_PATH=$ROOT_DIR/.docker/js

if [ -z "$VERSION" ]; then
  VERSION="24"
fi

source $ROOT_DIR/.docker/.shared/bin/apply
