#!/usr/bin/env bash

source $ROOT_DIR/.docker/.shared/bin/bootstrap

VERSION=$GRAPL_PYTHON_VERSION
APP_PATH=$ROOT_DIR/.docker/python

if [ -z "$VERSION" ]; then
  VERSION="3.14"
fi

source $ROOT_DIR/.docker/.shared/bin/apply
